# Production — points to Railway backend
# This file is safe to commit (no secrets — VITE_ vars are public at build time)
# Update this URL after Railway deployment, or set VITE_API_URL in Vercel dashboard
VITE_API_URL=https://taxlift-api.up.railway.app
